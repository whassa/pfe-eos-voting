import Head from "next/head";
export default function Header() {
  return (
    <Head>
        <title>Eos Proposals</title>
        <meta name="description" content="Eos Proposals is a voting platform using the blockchain eos. It's purpose is to show votes and be a hub where users can vote on different subject." />
        <link rel="icon" href="/favicon.ico" />
    </Head>
  );
}
