import { makeStyles } from "@mui/material/styles";

const useStyles = makeStyles((theme) => ({
  root: {
    width: "100%",
    display: "flex",
  },
  cardDescription: {
    marginBottom: "16px",
  },
}));

export default useStyles;
